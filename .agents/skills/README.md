# Project skills

Run `tools/setup_ai_dev.ps1` to install the pinned MaaFramework skill into this
directory. Downloaded skill contents are intentionally ignored by Git; their source and
commit are recorded in `tools/ai-tools.lock.json`.
